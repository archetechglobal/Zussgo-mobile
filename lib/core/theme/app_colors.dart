import 'package:flutter/material.dart';

class AppColors {
  // ── Backgrounds ───────────────────────────────────
  static const Color bg             = Color(0xFF050C0D);
  static const Color background     = Color(0xFF050C0D); // alias for bg
  static const Color surface        = Color(0x09FFFFFF);
  static const Color surface1       = Color(0xFF12161D); // alias for surface2
  static const Color surface2       = Color(0xFF12161D);
  static const Color surface3       = Color(0xFF181D26);
  static const Color border         = Color(0x14FFFFFF);
  static const Color borderFocus    = Color(0x6658DAD0);

  // ── Brand ─────────────────────────────────────────
  static const Color primary        = Color(0xFF58DAD0);
  static const Color primary2       = Color(0xFF58DAD0); // alias for primary
  static const Color primaryDark    = Color(0xFF3EC4BA);

  // ── Accents ───────────────────────────────────────
  static const Color gold           = Color(0xFFF7B84E);
  static const Color coral          = Color(0xFFFF8A6C);
  static const Color rose           = Color(0xFFCF5A7E);

  // ── Text ──────────────────────────────────────────
  static const Color text           = Color(0xFFEDF7F4);
  static const Color textSub        = Color(0x61EDF7F4);
  static const Color textMuted      = Color(0x59EDF7F4);
  static const Color faint          = Color(0x38EDF7F4); // alias for textFaint
  static const Color textFaint      = Color(0x38EDF7F4);
  static const Color textHint       = Color(0x40EDF7F4);
  static const Color textInverse    = Color(0xFF050C0D);

  // ── Interactive ───────────────────────────────────
  static const Color switchLink     = Color(0x47FFFFFF);
  static const Color tealLink       = Color(0xFF58DAD0);
  static const Color tealLinkMid    = Color(0xCC58DAD0);
  static const Color tealSoft       = Color(0x0A58DAD0);
  static const Color tealBorder     = Color(0x1A58DAD0);

  // ── Shadows ───────────────────────────────────────
  static const Color shadowTeal1    = Color(0x3858DAD0);
  static const Color shadowTeal2    = Color(0x2658DAD0);

  // ── Semantic ──────────────────────────────────────
  static const Color success        = Color(0xFF79C66B);
  static const Color error          = Color(0xFFFF6B6B);
  static const Color warning        = Color(0xFFF7B84E);
}